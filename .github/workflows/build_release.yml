name: Release Build

on:
  workflow_dispatch:
    inputs:
      TOOLCHAINS:
        type: choice
        description: "使用的工具链"
        required: true
        default: "nightly"
        options:
          - "nightly"
          - "stable"
      TARGET:
        type: choice
        description: "编译的目标三元组"
        required: true
        default: "x86_64-unknown-linux-gnu"
        options:
          - "x86_64-unknown-linux-gnu"
          - "aarch64-unknown-linux-gnu"
          - "x86_64-unknown-linux-musl"
          - "aarch64-unknown-linux-musl"
          - "x86_64-pc-windows-msvc"
          - "aarch64-pc-windows-msvc"
          - "x86_64-apple-darwin"
          - "aarch64-apple-darwin"
      USE_O2:
        type: boolean
        description: "是否使用O2编译？(默认O3)"
        required: true
        default: false

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  # Optimize build performance
  CARGO_INCREMENTAL: 0
  TZ: Asia/Shanghai

jobs:
  # Build strategy check - determine build type based on trigger
  build-check:
    name: Build Strategy Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Debug Show Selected Inputs
        run: |
          echo "-----------------------"
          echo "Selected TOOLCHAINS: ${{ github.event.inputs.TOOLCHAINS }}"
          echo "Selected TARGET: ${{ github.event.inputs.TARGET }}"
          echo "-----------------------"

  # Build RustFS binaries
  build-utils:
    name: Build Utils
    needs: [build-check]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux builds
          - os: ${{ contains(github.event.inputs.TARGET, 'windows') && 'windows' || (contains(github.event.inputs.TARGET, 'apple') && 'macos' || 'ubuntu') }}-${{ contains(github.event.inputs.TARGET, 'linux') && '24.04' || contains(github.event.inputs.TARGET, 'apple') && 'latest' || contains(github.event.inputs.TARGET, 'aarch64') && '11' || 'latest' }}${{ contains(github.event.inputs.TARGET, 'aarch64') && !contains(github.event.inputs.TARGET, 'apple') && '-arm' || '' }}
            target: ${{ github.event.inputs.TARGET }}
            cross: ${{ contains(github.event.inputs.TARGET, 'linux') && 'true' || 'false' }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Rust environment
        uses: ./.github/actions/setup
        with:
          rust-version: ${{ github.event.inputs.TOOLCHAINS }}
          target: ${{ matrix.target }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          install-cross-tools: ${{ matrix.cross }}

      - name: Configure Git
        shell: bash
        run: |
          git config --global user.name "Numbersf"
          git config --global user.email "263623064@qq.com"

      - name: Setup TimeZone
        if: runner.os == 'Linux'
        run: |
          sudo -E rm -rf /etc/localtime
          sudo -E ln -s /usr/share/zoneinfo/Asia/Shanghai /etc/localtime

      - name: Clone utils sources
        shell: bash
        run: |
          for i in "astral-sh/uv"; do
 
              git clone --depth 1 https://github.com/$i

              cd "$(basename $i)"
              rm -rf .git rust-toolchain.toml || true
              cd  ..
          done

      - name: Optimize upstream mallocator and optmize level
        shell: bash
        run: |         
          git clone --depth 1 https://github.com/reigadegr/uv_action self_code
          rm -rf self_code/.git .git || true
          git init
          mkdir -p output || true
          echo "flag" > output/flag.txt
          if [ "${{ github.event.inputs.USE_O2 }}" = "true" ]; then
              sed -i 's/+opt-level = 3/+opt-level = 2/g' ./self_code/patchs/*
          fi
          touch output/failure.log
          git add  . && git commit -m "auto commit" >/dev/null 2>&1 || echo "无需提交"
          for i in `realpath ./self_code/patchs/*.patch`; do
             patch -p1 -F 3 < $i && git add  . && git commit -m "$i成功应用" || echo "$i应用失败" >>output/failure.log
             git reset --hard
             git clean -df
          done
          rm -f output/flag.txt

      - name: "⚙️ Reset flags"
        shell: bash
        run: |
          rm -rf .cargo/ || echo "无需删除目录"
          mkdir -p .cargo/
          curl -L https://github.com/reigadegr/uv_action/raw/main/.cargo/config.toml -o .cargo/config.toml

      - name: Build Rust Utils
        shell: bash
        run: |
          rm -rf ./*/target && echo "删除了target" || echo "无需删除"
          work_space="$(pwd)"
          mkdir output || true
          rm -rf ~/.cargo/git ~/.cargo/registry ~/.cache || true
          cd uv
          for bin_name in "uv" "uvx" "uvw"; do
              echo "当前编译的项目: $bin_name"
              toolchain="${{ github.event.inputs.TOOLCHAINS }}"
              platform="${{ contains(github.event.inputs.TARGET, 'windows') && 'windows' || (contains(github.event.inputs.TARGET, 'apple') && 'apple' || 'linux') }}"
              script_file="build_"$toolchain"_"$platform".sh"

              url="https://github.com/reigadegr/uv_action/raw/main/script/$script_file"
              echo "脚本直链: $url"
              curl -L $url -o "$script_file"
              # musl build
              is_musl="${{ contains(github.event.inputs.TARGET, 'musl') && 'true' || 'false' }}"
              if [ "$is_musl" = "true" ]; then
                  sed -i 's/ build / zigbuild /g' "$script_file"
                  sed -i 's/,--relax//g' "$script_file"
                  sed -i 's/relocation-model=pic/relocation-model=static/g' "$script_file"
              fi
              sh "$script_file" "${{ matrix.target }}" "$bin_name" || continue

              is_windows="${{ contains(github.event.inputs.TARGET, 'windows') && 'true' || 'false' }}"
              if [ "$is_windows" = "true" ]; then
                  bin_name=$bin_name.exe
              fi
              file="$(find target -name "$bin_name")"
              echo "$(realpath $file)"              
              dd if=target/"${{ matrix.target }}"/release/$bin_name of=../output/$bin_name
              # rm -rf target
          done

      - name: Upload to GitHub artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.event.inputs.TARGET }}_uv_${{ github.event.inputs.TOOLCHAINS }}
          path: output/*
          retention-days: 2
